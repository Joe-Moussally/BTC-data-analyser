1- executing 'pip install pandas' in cmd to install 'pandas' package

2- executing 'pip install openpyxl' in cmd

3- To execute the script for reading a specific excel file, execute the following command in cmd (same directory as the python file):

python BTC-Analyser.py 'excel file path'